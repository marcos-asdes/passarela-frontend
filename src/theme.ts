/** Tema compartilhado entre o `ConfigProvider` do antd e o `ThemeProvider` do styled-components. */
export const theme = {
  colors: {
    primary: '#1677ff',
    text: 'rgba(0, 0, 0, 0.88)'
  }
}

export type AppTheme = typeof theme
